# Temporary diagnostic: dispatch this to surface reusable-workflow validation errors.
name: OpenSpec smoke test
on:
  workflow_dispatch:
jobs:
  openspec:
    uses: Jam-Sw/.github/.github/workflows/validate-openspec.yml@main
